<mvc:View
	controllerName="com.meridian.partners.controller.Partners"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns="sap.m">
	<!-- Intentionally almost empty. The toolbar + table are built imperatively in the
	     controller's onInit (new sap.m.Table(...) etc.) instead of being declared here.
	     Migration item BP-03: move all of this into the XML. -->
	<Page id="partnerPage" title="Business Partners" busy="{data>/busy}">
		<content>
			<!-- content injected at runtime by Partners.controller.js#onInit -->
		</content>
	</Page>
</mvc:View>
